# cocotb and forastero example

Currently testing with forastero-io as a local git clone.

```
git clone --branch symbol_fixups https://github.com/simpsoneric/forastero-io.git
```

Tests:

```bash
uv run pytest -s
```
